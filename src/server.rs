//! `rmcp` Server scaffold — placeholder for v0.1-05.
//!
//! The full implementation in v0.1-05 wires the `rmcp` Server trait,
//! advertises the supported MCP capabilities (tools, resources,
//! logging), and pins the spec revision (`2025-06-18`).

/// Placeholder marker for the future server type.
///
/// Replaced in v0.1-05 with the real `rmcp` Server implementation.
#[derive(Debug, Default, Clone, Copy)]
pub struct ServerStub;
